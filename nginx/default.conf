server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Try the file/dir; if missing, redirect back to /
    location / {
        try_files $uri $uri/ @redirect;
    }

    location @redirect {
        return 302 /;
    }
}
